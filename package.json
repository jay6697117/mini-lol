{
  "name": "mini-lol",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "optimize:assets": "node scripts/optimize-runtime-assets.mjs",
    "build": "npm run optimize:assets && tsc --noEmit && vite build",
    "preview": "vite preview",
    "test:mvp": "npm run build && node playtest-artifacts/phase-1-simulation-unit/assertions.mjs && node scripts/mvp-test.mjs"
  },
  "dependencies": {
    "phaser": "3.90.0"
  },
  "devDependencies": {
    "playwright": "^1.60.0",
    "typescript": "^6.0.3",
    "vite": "^8.0.12"
  }
}
